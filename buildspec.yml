version: 0.2

phases:
  install:
    runtime-versions:
        java: openjdk8
  pre_build:
    commands:
  build:
    commands:
        - echo Building...
		- mvn clean prepare-package war:exploded
        - echo Finished build.
  post_build:
    commands:
       - echo "Entered the post-build phase"
       - mv target/deploy ./
artifacts:
  files:
    - deploy/**/*